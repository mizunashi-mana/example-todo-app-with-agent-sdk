{
  "name": "example-todo-app-root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @example-todo-app/eslint-config && npm run build -w @example-todo-app/todo-app && npm run build -w @example-todo-app/web-ui",
    "dev": "concurrently -n server,web-ui -c blue,green \"npm run dev -w @example-todo-app/todo-app\" \"npm run dev -w @example-todo-app/web-ui\"",
    "lint": "prek run --all-files",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "Apache-2.0 OR MPL-2.0",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
